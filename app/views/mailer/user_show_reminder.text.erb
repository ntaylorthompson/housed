Mailer#user_show_reminder

Mailer#show_reminder

<%= @greeting %>,

Just a quick reminder that you're playing a show tomorrow (<%= @show.date %>) at <%= @host.name %>'s house: 

<%= @show.address %>

The music starts at <%= @show.time %>, so make sure to arrive in time to set up. 

Housed.com 